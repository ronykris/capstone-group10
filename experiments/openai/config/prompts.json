{
    "food_classification": {
        "v1": "Analyze the given image with dimensions {width}x{height} pixels. Detect and classify the food items in the image. For each detected item, estimate its macronutrient composition (protein, fat, and carbs), total calories, and provide the bounding box coordinates as absolute pixel values in the format (x1, y1, x2, y2) based on the image's resolution. Ensure the bounding boxes accurately enclose the food items.",
        "v2": "Analyze the food items in this image with dimensions {width}x{height} pixels. Provide a structured JSON response with the following requirements:\n\n1. 'image_dimensions': Exact image width and height as [width, height]\n2. 'food_items': An array of detected food items, where each item contains:\n   - 'name': Specific food item name\n   - 'macros': Precise macronutrient breakdown (protein, fat, carbs in grams)\n   - 'calories': Total calories\n   - 'bounding_box': Relative coordinates (0-1 range) [x1, y1, x2, y2]\n   - Optional 'confidence': Detection confidence (0-1 range)\n\nIf no food items are detected, return an empty 'food_items' array.",
        "v3": "Step 1: Begin by analyzing the provided image with dimensions {width}x{height} pixels. Identify and classify all visible food items in the image. Use precise object detection methods to locate each food item. Step 2: For each detected food item, execute the following substeps: a) Bounding Box Calculation: Determine the exact bounding box coordinates of the food item normalized relative to the original image dimensions. Output the coordinates as floating-point values in the range [0, 1] for (x1, y1, x2, y2), where: - (x1, y1) is the top-left corner. - (x2, y2) is the bottom-right corner. b) Macronutrient Estimation: Estimate the food item's macronutrient composition (protein, fat, and carbs in grams) based on its classification. c) Caloric Value: Calculate the total calories based on the estimated macronutrients. Step 3: Return the following structured output: image_dimensions: [width, height] food_items: A list where each item includes: - name: Name of the detected food item. - bounding_box: Bounding box coordinates as [x1, y1, x2, y2], normalized to the range [0, 1]. - macros: Macronutrient composition (protein, fat, carbs) in grams. - calories: Total caloric value in kcal. - confidence: Detection confidence score (0-1). Step 4: Verify the consistency of the bounding box by ensuring it aligns proportionally with the normalized image resolution and clearly encloses the detected food item. Refine the output if inconsistencies arise. Thought Process: - First, prioritize object detection accuracy by considering the food item’s visual features and edges. - Next, validate the normalized bounding box values to ensure they remain in the range [0, 1]. - Finally, map the classification results to nutritional estimations using a reliable food database or approximation logic."
    }
}